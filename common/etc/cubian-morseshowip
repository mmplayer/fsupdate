#!/bin/bash
ipaddr=`/etc/cubian-getip`
lockfile='/var/lock/cubian-morsemsg'
if [ "$ipaddr" != '' ];then
    if [ -f $lockfile ];then
        rm -f $lockfile
    fi
    /etc/cubian-morsemsg $ipaddr
fi
