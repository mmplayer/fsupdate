#!/bin/bash
ipaddr=`/usr/local/bin/cubian-getip`
ipaddr_reversed=`echo $ipaddr | rev`
lockfile='/var/lock/cubian-morsemsg'
if [ "$ipaddr" != '' ];then
    if [ -f $lockfile ];then
        rm -f $lockfile
    fi
    /usr/local/bin/cubian-morsemsg $ipaddr_reversed
fi
